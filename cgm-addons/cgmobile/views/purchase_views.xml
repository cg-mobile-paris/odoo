<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="purchase_order_form" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit.cgmobile</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form" />
        <field name="arch" type="xml">
            <xpath expr="//tree//field[@name='name']" position="after">
                <field name="barcode" optional="hide" />
            </xpath>
            <xpath expr="//field[@name='effective_date']" position="after">
                <field name="delivery_status" decoration-success="delivery_status == 'done'" decoration-info="delivery_status == 'in_progress'" decoration-danger="delivery_status == 'cancel'" widget="badge"/>
            </xpath>
        </field>
    </record>
</odoo>