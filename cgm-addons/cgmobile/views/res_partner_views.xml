<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="cgmobile_view_partner_form" model="ir.ui.view">
        <field name="name">cgmobile.res.partner.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="is_customer" invisible="1" />
                <field name="is_supplier" invisible="1" />
                <label class="o_form_label" for="is_customer" string="Type"
                       invisible="is_customer == False and is_supplier == False"/>
                <div>
                    <span class="badge badge-pill o_field_badge o_field_widget o_readonly_modifier bg-success-light" invisible="is_customer == False">Customer</span>
                    <span class="badge badge-pill o_field_badge o_field_widget o_readonly_modifier bg-info-light" invisible="is_supplier == False">Supplier</span>
                </div>
            </xpath>
        </field>
    </record>
</odoo>